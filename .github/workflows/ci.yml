name: GitHub Actions
on: [push]
jobs:
  ci:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install
        run: |
          sh -c "$(curl -fsLS get.chezmoi.io)" -- init --apply diohabara
